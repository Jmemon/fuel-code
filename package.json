{
  "name": "fuel-code",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test --recursive",
    "clean": "rm -rf packages/*/dist"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/node": "^22",
    "typescript": "^5.7"
  }
}
